import React from "react";
import { View } from "react-native";

// This screen is a dummy — tab press is intercepted in _layout.tsx
// to navigate to /screening/symptoms instead.
export default function ScanActionScreen() {
  return <View className="flex-1 bg-bg-light" />;
}
